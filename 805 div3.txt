#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long int t;
    cin >>t;
    while(t--){
        long long int n;
        cin >> n;
         long long int f=log10(n) + 1;

         long long int rem= n - (pow(10,f-1));

       cout << rem<< endl;

    }
}
