#include <bits/stdc++.h>

using namespace std;

int main()
{
   int n,a,b,cap=0,max=0;
   cin >> n;
   while(n--){
    cin >> a>>b;
    cap+= b-a;
    if(max<cap){
         max=cap;
    }
   }
   cout << max;
}

